---
name: vibe-website-builder
description: 从产品需求、信息架构、参考站研究、UI长页概念图、动效规格、技术栈与CMS选择，到网站开发、预览部署、生产验收和自媒体复盘的完整建站流程。用于新建或改版作品集、品牌站、内容站、活动站，以及包含登录和数据库但不含支付的Web App；尤其适用于用户要求先确认UI再写代码、比较主流低成本技术栈、研究参考网站、规划动效、选择CMS或审核网站是否可上线的任务。
---

# Vibe Website Builder

按确认门禁推进网站。不要把“能运行”“部署成功”或普通的“OK”当作阶段完成。

## 开始工作

1. 如项目已有仓库、线上网站、设计稿或任务记录，先只读检查事实。
2. 运行 `scripts/create_project_kit.py` 创建项目文档包；这不是网站代码。
3. 读取与当前阶段直接相关的参考文件：
   - 需求、页面与确认：`references/intake-and-gates.md`
   - 可直接使用的提示词：`references/prompt-playbook.md`
   - UI与动效：`references/ui-and-motion.md`
   - 技术、CMS与数据：`references/stack-cms-data.md`
   - 开发、验收与发布：`references/build-release.md`
   - 复盘和历史经验：`references/retrospective-and-lessons.md`
4. 每次只提出一个会改变方案的问题。问题必须具体，并给出推荐答案。
5. 将所有阶段、确认词和变更记录到 `project-state.json`。

## 不可绕过的确认词

确认顺序固定：

1. `需求确认`
2. `页面确认`
3. `UI确认`
4. `动效确认`
5. `技术确认`
6. `上线确认`

只有用户明确回复完全相同的确认词，才更新对应状态。`OK`、`可以`、`继续`、部署成功或测试通过都不能代替确认。

确认后的修改先输出：

- 原值
- 新值
- 受影响页面
- 受影响动效、数据和开发成本
- 需要退回的最早确认节点

只退回受影响节点及其后续节点。

## 固定工作流

### 1. 需求和页面

- 使用 `assets/project-brief.md` 收集人物/品牌、用户、目标、内容、维护方式、商业属性和Web App需求。
- 先输出用户任务、必须/暂缓功能、页面结构、真实内容需求、MVP标准和缺失素材。
- 此时不要选择技术栈，不要写网站代码。
- `需求确认` 后，根据网站类型自动推荐最重要的3个页面，并说明选择原因。
- `页面确认` 后，只做技术可行性和潜在成本预检，不锁定框架。
- 建立轻量DEMO内容模型。真实内容不完整时允许虚构内容，但必须标记 `DEMO` 并登记。

### 2. 参考研究和UI

- 除非用户已给参考站并明确说无需检索，否则根据项目选择3–5个集合站并实际检索。
- 整理1–3个案例，分别说明可借鉴的布局、字体、色彩、图片处理和动效。
- 只迁移设计规律。不得复制文字、图片、字体、品牌资产或未许可代码；开源代码先查许可证。
- 从已确认页面中制作3种明显不同的方向。每种方向包含相同3个页面，共9张1440px宽桌面长页图。
- 三种方向使用同一组核心DEMO内容与页面任务，但可改变导航、结构、布局、视觉和交互概念。
- 用户可混合各方向元素。将选择固化到 `assets/design-recipe.md`。
- 最终生成3张1440px宽桌面长页、3张390px宽手机长页及关键区域放大图。
- 优先使用Figma制作精确稿；未连接Figma时，使用长页图片并附完整数值规范。
- `UI确认` 前禁止创建或修改HTML、CSS、React、Vue、Astro、Next.js页面或浏览器原型。图片生成、Figma、研究、文档和辅助整理脚本允许。

### 3. 动效

- `UI确认` 后才研究动效。
- 按项目选择3–5个动效资源平台，实际整理1–3个案例。
- 提供克制、叙事、实验三档方案，允许混合。
- 所有动效先填写 `assets/motion-spec.md`：触发、起止状态、持续时间、easing、滚动关系、移动端降级、reduced-motion和性能风险。
- 最多为3个关键动效制作Figma原型或短样片，其余使用分镜。
- `动效确认` 前不写网站实现代码。

### 4. 技术、CMS和数据

- 在UI与动效确定后比较原生HTML/CSS/JS、Astro、Vite+React、Vite+Vue和Next.js。
- 从SEO、动效、维护、部署、学习、长期成本和已确认需求判断，最后只推荐一个。
- 使用 `references/stack-cms-data.md` 的明确规则选择CMS，不默认安装CMS。
- 在 `技术确认` 前完成：
  - 首年成本表
  - CMS和内容更新流程
  - 正式内容Schema与TypeScript类型
  - Web App需要的数据库、Auth和RLS设计
  - 部署方案
- 未经用户确认不得启用可能收费的服务。
- Python用于批量导入、图片整理、审计和部署验证，不作为默认网页生成器。

### 5. 开发

- `技术确认` 后才能创建网站代码。
- 先开发一个代表页面，不加复杂动效。
- 在1440px、1024px和390px宽度截图，与确认稿逐项比较并报告明确数值差异。
- 代表页面通过后再扩展全部静态页面。
- 静态页面稳定后，每次只实现一种已确认动效；该次不得同时改内容、布局和其他动效。
- 代码中将沟通使用的px转换为合适的rem、clamp和设计变量。

### 6. 预览、验收和发布

- 运行内容校验和发布目录审计。
- 创建预览部署，不直接发布正式域名。
- 在真实浏览器检查桌面和手机，不能只看构建成功。
- 生产阻断项见 `references/build-release.md`。阻断项未清零时禁止请求 `上线确认`。
- 用户回复 `上线确认` 后才发布正式域名。
- 使用原始字节进行上传；禁止把终端显示内容当成完整文件。
- 对允许列表中的线上文件与本地发布包逐字节比较，再做生产URL浏览器回归。

### 7. 复盘

- 发布后生成完整自媒体内容包，使用 `references/retrospective-and-lessons.md`。
- 至少包含时间线、关键Prompt、做对/踩坑、前后对比、技术理由、成本，以及5个选题的标题、钩子、大纲和配图建议。

## 数值沟通

禁止使用“大一点”“小一点”“紧凑一些”等无数值措辞。

- 明确给出字号、行高、间距、宽度、圆角、边框、图片比例、位移、持续时间和easing。
- 用户不确定时，给两个明确数值让其选择，或说明采用二者中间值。
- 每次变更记录旧值、新值和影响范围。

## 可复用脚本

所有脚本仅使用Python标准库，可在Windows运行：

```powershell
py -3 scripts/create_project_kit.py --output <项目文档目录>
py -3 scripts/validate_content.py --content-root <内容目录>
py -3 scripts/audit_release.py --site-root <发布目录>
py -3 scripts/verify_deployment.py --site-root <发布目录> --base-url <预览或生产URL> --allowlist <allowlist.json>
```

先阅读脚本开头“用户配置区”。脚本失败时修复原因，不绕过检查。

## GitHub手动同步

禁止安装后台监控、计划任务或登录启动项。只有用户明确要求“同步到GitHub”时才运行：

```powershell
py -3 scripts/sync_to_github.py --message "说明本次修改"
```

脚本先运行Skill校验和密钥/大文件扫描，再提交并推送。远端存在新提交、校验失败或疑似密钥时停止，不强制覆盖。
